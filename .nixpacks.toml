[layers]
# Optimize for Go builds
setupPhase = """
  cd web-app
"""
buildPhase = """
  echo "📦 Installing dependencies..."
  go mod download
  
  echo "🔨 Building IPO Pilot..."
  go build -o ipo-pilot .
  
  echo "✓ Build complete!"
"""

[start]
cmd = "cd web-app && ./ipo-pilot"

[variables]
GO_VERSION = "1.21"
